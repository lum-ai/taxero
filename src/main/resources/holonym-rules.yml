vars:
  result: "(?<result> [tag=/J.*/]{,3} [tag=/N.*/]+ (of [tag=DT]? [tag=/J.*/]{,3} [tag=/N.*/]+)?)"
  skipquantifier: "/(most|less|more|few|any|much|many|lots?|little|some|same|several|numerous|plenty|amounts?|major|minor|majority|number|multiple|range|variet(y|ies)|others?|additional|next|basic|whole|of|such|all|essential|necessary|specific|special|particular|certain|importan(t|ce)|significant|main|extreme|different|various|unusual|usual|common|general|familiar|small(er|est)?|big(ger|gest)?|large(r|st)?|old(er|est)?|young(er|est)?|new(er|est)?|own|event|types?|kinds?|examples?|sorts?|class(es)?|parts?|pieces?|members?|aspects?|components?|sets?|an?|the|wide|only)/{,5}"

rules:
  - name: holo-1
    type: basic
    example: "PART is a (member|part) of WHOLE"
    pattern: |
      ${result}
      <nsubj
      [lemma=/(member|part|component|section|department|subdivision|region)/]
      >/nmod_(of|in)/
      ${skipquantifier}
      ${query}

  - name: holo-2
    type: basic
    example: "WHOLE is made of PART"
    pattern: |
      ${result}
      </nsubj(pass)?/
      made
      >/nmod_of|nmod_out_of|nmod_from/
      ${skipquantifier}
      ${query}

  - name: holo-3
    type: basic
    example: "WHOLE consists of PART"
    pattern: |
      ${result}
      <nsubj
      [lemma=consist]
      >nmod_of
      ${skipquantifier}
      ${query}

#  - name: holo-4
#    type: basic
#    example: "WHOLE's PART"
#    # Berland & Charniak (1999)
#    pattern: |
#      ${result}
#      "'s"
#      ${skipquantifier}
#      ${query}

#  - name: holo-5
#    type: basic
#    example: "PART of (the|a) WHOLE"
#    # Berland & Charniak (1999)
#    pattern: |
#      ${query}
#      <nmod_of
#      ${skipquantifier}
#      ${result}

  - name: holo-6
    type: basic
    example: "WHOLE has four PART"
    # Berland & Charniak (1999)
    pattern: |
      ${result}
      <nsubj
      (have|has)
      >dobj
      ${skipquantifier}
      ${query}

  - name: holo-7
    type: basic
    example: "WHOLE comprises PART"
    pattern: |
      ${result}
      [lemma=comprise]
      ${skipquantifier}
      ${query}

